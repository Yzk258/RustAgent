说明：本帖文案由 AI 辅助撰写

一、项目背景与现实痛点

Minecraft Java 版的模组生态极其庞大，仅 Modrinth 平台就收录了上万个模组。玩家在与朋友游玩时常常面临两个现实困扰：一是现有模组或整合包玩腻之后，不知道还有哪些质量高、合胃口的新模组，相关信息的获取基本依赖自己在社区里大海捞针，或是观看博主的视频解说，效率低且高度依赖运气；二是自己动手配置整合包时，经常被模组之间的不兼容问题困扰，需要逐个打开模组页面，人工核对 Minecraft 版本、Fabric/Forge/NeoForge 加载器、前置依赖等信息，一次整合往往要反复试错数小时。这种"不知道玩什么、也配不明白"的困境，让大量优质模组内容被埋没，也让玩家的尝试成本居高不下。

二、通用 Agent 与传统工具的局限

现有的 Modrinth App、CurseForge App、Prism Launcher 等工具已经能完成整合包的下载与安装，但它们只解决"装"的问题，不解决"选"的问题——既没有基于个人口味的内容发现能力，也不会在组包时对兼容性做主动检查，版本冲突依然要玩家自己踩坑。通用大语言模型虽然能理解玩家的自然语言描述，但直接用于模组管理存在明显短板。首先，模型不了解 Modrinth 上的实时数据，推荐结果要么过时、要么是凭空幻觉出来的模组名；其次，模型缺乏精确的版本比对与依赖解析能力，容易推荐版本不匹配或缺少前置的模组；更关键的是，模型无法记住玩家的历史偏好与反馈，每次对话都从零开始。这使得通用 Agent 只能做"一次性模糊推荐"，无法沉淀为长期可用的模组管理助手。

三、项目整体定位

本项目基于 Rust 开发一个面向 Minecraft 模组管理的专用 Agent。Agent 通过 Modrinth 公开 API 获取模组的实时元数据（版本、加载器、依赖、下载量、标签、简介等），核心的检索、过滤、整合包生成与冲突检测逻辑全部由 Rust 实现；LLM 负责理解玩家的自然语言需求、协调工具调用，并将结构化结果转化为用户可读的推荐说明。玩家的完整使用流程是：给出版本信息与游玩偏好（也可点名指定某些必须包含的 mod）→ Agent 检索并给出带描述的候选列表，先不下载，由用户逐个筛选 → 筛选反馈沉淀进本地用户数据库 → Agent 组包并完成冲突元检测 → 生成 Modrinth 标准格式的 .mrpack 整合包文件，拖入 PCL2 等启动器即可一键安装启动。LLM 接口支持本地部署模型与 API Key 远程调用两种方式，用户可自由切换。

四、场景专属定制能力

针对通用 Agent 的缺陷，本项目设计了三项专属定制：

第一，基于 Modrinth 元数据的结构化过滤链。 本项目将"游戏版本、加载器、内容类型、特色标签"等约束固化为一套由 Rust 实现的结构化过滤工具链：先由 Rust 直接调用 API 获取真实元数据并逐级筛除不符合要求的模组（包括 client/server 端环境适配标记），用户点名指定的 mod 也在此环节被精确检索与版本锁定；再把筛后的候选集交给 LLM 做排序与描述，而不是让模型凭训练记忆直接推荐。所有过滤判定都基于 API 返回的结构化数据，保证"推荐出来的每一个模组都真实存在、版本可用"，从根本上解决通用 LLM 推荐幻觉与版本错配的问题。

第二，用户数据库与"试试这个"主动推荐。 所有整合包整理记录与玩家的喜欢/不喜欢反馈都按结构化形式持久化到本地用户数据库，形成随时间增长的口味数据（偏好标签、题材倾向、玩法偏好等）；用户点名指定的 mod 同样是高价值的口味信号，缓解冷启动问题。"试试这个"窗口定期获取 Modrinth 上的最新与热门模组，先在 Rust 侧依据用户数据库完成初筛与排序，再由 LLM 结合玩家历史反馈生成个性化推荐语。更进一步，本项目的推荐结果可以直接渲染为 PCL2 启动器主页的自定义卡片（PCL2 官方支持基于 XAML 的主页个性化），让推荐出现在玩家每天打开启动器的第一屏——"推荐、下载、启动"在同一个界面闭环。

第三，冲突元检测。 生成整合包时，Rust 侧基于模组元数据自动检查游戏版本一致性、加载器兼容性、前置依赖完整性、client/server 环境适配以及元数据层面的冲突声明，在筛选阶段即对每个候选标注"与已有模组的冲突风险"，组包阶段再做整体校验兜底，直接标记出问题模组并给出具体原因。由于全部判定基于可复现的结构化数据，检测结果稳定、可解释，且每次组包的成本几乎为零。

五、实现路线与技术验证

据本项目初步实测：Modrinth API 检索与元数据过滤已在 Rust 中实现并验证返回数据的完整性（标题、版本、加载器、分类等字段齐全）；组包模块按 Modrinth 官方 .mrpack 规范生成整合包索引（含 SHA1/SHA512 校验、下载地址与依赖声明），生成的 .mrpack 已实测拖入 PCL2 启动器成功识别并自动完成下载安装，"Agent 生成 → 启动器一键安装"的端到端链路得到确认。由于 .mrpack 只存储元数据（约 KB 级），实际 mod 下载由启动器完成，Agent 自身无需处理大文件与下载加速，工程复杂度可控。Rust 生态中的 reqwest、serde、tokio、zip 等库支撑异步 API 编排、结构化解析与整合包打包，技术选型均已验证。

本项目可从以下维度进行评估：模组检索命中率（返回结果与玩家自然语言描述的相关性）；版本与加载器过滤准确率（关键元数据字段 100% 准确应用）；冲突元检测的查全率与查准率（在已知冲突样例集上的表现）；推荐相关性（用户对"试试这个"推送的主观评分）；端到端响应延迟（整包生成控制在分钟级以内）与多轮交互稳定性。
